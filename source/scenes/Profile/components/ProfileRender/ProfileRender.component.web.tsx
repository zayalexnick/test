import * as React from 'react';
import { Container, Text } from '../../styles/Profile.styles.web';

const ProfileRender: React.SFC = () => (
    <Container>
        <Text>Профиль</Text>
    </Container>
);

export default ProfileRender;